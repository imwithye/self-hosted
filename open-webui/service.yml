apiVersion: v1
kind: Service
metadata:
  namespace: open-webui
  name: open-webui
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: open-webui
